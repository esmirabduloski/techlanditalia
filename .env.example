# Copy to .env and fill with your Lovable Cloud values.
# Publishable/anon keys are safe to expose in the client bundle (protected by RLS).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=
